import HTMLHead from '../components/HTMLHead'

const Layout = ({ children }) => (
  <>
    <HTMLHead></HTMLHead>
    {/* TODO: add default layout component */}
    {children}
  </>
)

export default Layout
